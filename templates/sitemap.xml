<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%for i in range(0, len)%} 
   <url>
        <loc>{{ pages[i]['url']}}</loc>
        <lastmod>{{ pages[i]['lastmod'] }}</lastmod>
        <changefreq>{{ pages[i]['changefreq'] }}</changefreq>
        <priority>{{ pages[i]['priority'] }}</priority>
   </url>
{%endfor%} 
</urlset> 

